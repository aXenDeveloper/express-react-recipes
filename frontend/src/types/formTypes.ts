export type RegisterViewFormValues = {
  displayName: string;
  email: string;
  password: string;
  passwordCF: string;
};

export type LoginViewFormValues = {
  email: string;
  password: string;
};
