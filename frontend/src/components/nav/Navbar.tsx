import NavItems from './NavItems';

const NavBar = () => (
  <nav className="nav">
    <div className="container">
      <ul>
        <NavItems />
      </ul>
    </div>
  </nav>
);

export default NavBar;
